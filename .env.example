# RedRat Proxy - Production Environment Configuration
# Copy this file to .env and update with your settings

# Database Configuration
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=redrat_proxy
MYSQL_USER=redrat
MYSQL_PASSWORD=your_secure_password_change_this

# Application Configuration (Production)
SECRET_KEY=your_secure_secret_key_change_this_in_production
UPLOAD_FOLDER=app/static/uploads
LOG_LEVEL=INFO

# Web Configuration
SERVER_NAME=localhost:5000
APPLICATION_ROOT=/
PREFERRED_URL_SCHEME=http

# RedRat Device Configuration (Optional - for backward compatibility)
# Note: Modern deployments use RedRat devices configured in the database
# These variables are only used if no RedRat devices are configured in the database
# REDRAT_HOST=192.168.1.100
# REDRAT_PORT=10001
REDRAT_XMLRPC_URL=http://your-redrat-ip:40000/RPC2

# Optional: Advanced Configuration
# FLASK_DEBUG=False (automatically set to False in production)
# FLASK_RUN_HOST=0.0.0.0 (automatically set)
# FLASK_RUN_PORT=5000 (automatically set)
# PYTHONPATH=/app (automatically set)
